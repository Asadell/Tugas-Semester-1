#include <stdio.h>

int main(){
	float discount;
	
	scanf("%f", &discount);
	
	printf("%f", discount);
}
/*
5.Gunakan pernyataan printf() untuk menampilkan (di layar) nilai dari sebuah variabel float 
(misalkan namanya = discount).
*/
